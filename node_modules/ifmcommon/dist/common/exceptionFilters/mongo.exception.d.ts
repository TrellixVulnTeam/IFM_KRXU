import { ArgumentsHost, ExceptionFilter } from '@nestjs/common';
import { KafkaConfig } from 'kafkajs';
import { MongoError } from 'mongodb';
import { PostKafka } from '../queueService/post-kafka';
export declare class MongoExceptionFilter implements ExceptionFilter {
    private readonly i18n;
    postKafka: PostKafka;
    exceptionTopic: any;
    constructor(i18n: any, kafkaConfig: KafkaConfig, exceptionTopic: any);
    private logger;
    catch(exception: MongoError, host: ArgumentsHost): Promise<void>;
}
